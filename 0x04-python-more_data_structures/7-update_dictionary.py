#!/usr/bin/python3

def update_dictionary(a_dictionary, key, value):
    """The function will eplace or add key/value pairs in a dictionary."""
    a_dictionary[key] = value
    return (a_dictionary)
